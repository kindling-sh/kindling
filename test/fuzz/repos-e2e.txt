# ─────────────────────────────────────────────────────────────────
# repos-e2e.txt — Repos for full e2e fuzz testing (Kind cluster)
#
# Selection criteria:
#   - Self-contained Dockerfiles (build from fresh clone, no pre-steps)
#   - Simple, well-maintained repos with real backing dependencies
#   - Diverse languages (Python, Node, Go/.NET)
#   - Each Dockerfile in the repo root or a service subdirectory
# ─────────────────────────────────────────────────────────────────

# Docker's official multi-service example — vote (Python), result (Node), worker (.NET)
# 3 services, each with its own Dockerfile, uses redis + postgres
https://github.com/dockersamples/example-voting-app

# Node.js Express + MongoDB — single-service, root Dockerfile, docker-compose
https://github.com/hagopj13/node-express-boilerplate

# Python Flask + SQLite/Postgres — single-service, root Dockerfile
https://github.com/miguelgrinberg/microblog
